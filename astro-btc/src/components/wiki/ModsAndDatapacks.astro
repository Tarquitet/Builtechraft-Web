---
// src/components/wiki/ModsAndDatapacks.astro
---
<style>
    .mod-item:nth-child(odd) { align-self: flex-start; margin-top: 0.5rem; }
    .mod-item:nth-child(even) { align-self: flex-end; margin-bottom: 0.5rem; }
    .hide-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
    .hide-scrollbar::-webkit-scrollbar { display: none; }
</style>

<div id="loading-mods" class="flex flex-col justify-center items-center py-10 mb-10 text-theme-muted gap-3">
    <span class="text-3xl animate-bounce">📦</span>
    <span class="animate-pulse font-bold tracking-widest uppercase text-sm">Sincronizando base de datos...</span>
</div>

<div id="datapacks-section" class="hidden flex-col mb-16 relative z-20">
    <div class="mb-6 flex items-center gap-3 border-b border-theme pb-4">
        <span class="text-2xl">🗂️</span>
        <h3 class="text-xl font-display font-bold text-btc-yellow uppercase tracking-widest m-0">Datapacks Instalados</h3>
    </div>
    
    <div class="relative w-full group/slider">
        <button id="dp-prev" class="absolute left-0 top-1/2 -translate-y-1/2 -ml-4 md:-ml-6 z-10 bg-theme-card text-theme-main p-3 rounded-full border border-theme opacity-0 group-hover/slider:opacity-100 transition-opacity disabled:opacity-0 hidden md:flex items-center justify-center hover:bg-btc-orange hover:border-btc-orange hover:text-white shadow-lg cursor-pointer">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path></svg>
        </button>

        <div id="datapacks-list" class="flex overflow-x-auto snap-x snap-mandatory gap-5 pb-4 hide-scrollbar h-[420px] px-2 py-4"></div>

        <button id="dp-next" class="absolute right-0 top-1/2 -translate-y-1/2 -mr-4 md:-ml-6 z-10 bg-theme-card text-theme-main p-3 rounded-full border border-theme opacity-0 group-hover/slider:opacity-100 transition-opacity disabled:opacity-0 hidden md:flex items-center justify-center hover:bg-btc-orange hover:border-btc-orange hover:text-white shadow-lg cursor-pointer">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
        </button>
    </div>
</div>

<div id="mods-section" class="hidden flex-col mb-20 relative z-20">
    <div class="mb-6 flex items-center gap-3 border-b border-theme pb-4">
        <span class="text-2xl">⚙️</span>
        <h3 class="text-xl font-display font-bold text-blue-500 uppercase tracking-widest m-0">Mods del Servidor</h3>
    </div>
    
    <div class="relative w-full group/slider">
        <button id="mod-prev" class="absolute left-0 top-1/2 -translate-y-1/2 -ml-4 md:-ml-6 z-10 bg-theme-card text-theme-main p-3 rounded-full border border-theme opacity-0 group-hover/slider:opacity-100 transition-opacity disabled:opacity-0 hidden md:flex items-center justify-center hover:bg-btc-orange hover:border-btc-orange hover:text-white shadow-lg cursor-pointer">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path></svg>
        </button>

        <div id="mods-list" class="flex overflow-x-auto snap-x snap-mandatory gap-5 pb-4 hide-scrollbar h-[420px] px-2 py-4"></div>

        <button id="mod-next" class="absolute right-0 top-1/2 -translate-y-1/2 -mr-4 md:-ml-6 z-10 bg-theme-card text-theme-main p-3 rounded-full border border-theme opacity-0 group-hover/slider:opacity-100 transition-opacity disabled:opacity-0 hidden md:flex items-center justify-center hover:bg-btc-orange hover:border-btc-orange hover:text-white shadow-lg cursor-pointer">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
        </button>
    </div>
</div>

<script>
import { getModsAndDatapacks } from '../../utils/sheetsClient.js';
document.addEventListener('DOMContentLoaded', async () => {
    const dpList = document.getElementById('datapacks-list') as HTMLElement;
    const modsList = document.getElementById('mods-list') as HTMLElement;
    const dpSection = document.getElementById('datapacks-section') as HTMLElement;
    const modsSection = document.getElementById('mods-section') as HTMLElement;
    const loadingEl = document.getElementById('loading-mods') as HTMLElement;

    const getColorClass = (tipo: string) => {
        const t = tipo ? tipo.toLowerCase() : '';
        if(t.includes('mod')) return { text: 'text-blue-500', border: 'border-blue-500/50', badge: 'bg-blue-500/10 text-blue-500 border-blue-500/20' };
        if(t.includes('plugin')) return { text: 'text-orange-500', border: 'border-orange-500/50', badge: 'bg-orange-500/10 text-orange-500 border-orange-500/20' };
        if(t.includes('proxy')) return { text: 'text-emerald-500', border: 'border-emerald-500/50', badge: 'bg-emerald-500/10 text-emerald-500 border-emerald-500/20' };
        if(t.includes('datapack')) return { text: 'text-btc-yellow', border: 'border-btc-yellow/50', badge: 'bg-yellow-500/10 text-btc-yellow border-btc-yellow/20' };
        return { text: 'text-theme-muted', border: 'border-theme-muted/50', badge: 'bg-theme-alternate text-theme-muted border-theme' };
    };

    const createModCardHTML = (item: any) => {
        const colors = getColorClass(item.Type);
        const iconSrc = item.Icon 
            ? `<img src="${item.Icon}" alt="${item.Name}" class="w-full h-full object-contain drop-shadow-lg transition-transform duration-500 group-hover/card:scale-105" />` 
            : `<div class="w-full h-full flex items-center justify-center text-theme-muted text-4xl font-bold opacity-30">?</div>`;
        const maxChars = 85; 
        const fullDesc = item.Desc || '';
        const isTruncated = fullDesc.length > maxChars;
        const displayDesc = isTruncated ? fullDesc.substring(0, maxChars).trim() + '...' : fullDesc;
        
        return `
            <div class="mod-item snap-center shrink-0 w-60 flex flex-col bg-theme-card border border-theme rounded-2xl overflow-hidden hover:${colors.border} transition-colors duration-300 shadow-lg group/card">
                
                <div class="w-full h-32 bg-black/10 dark:bg-black/40 relative flex items-center justify-center border-b border-theme p-4">
                    ${iconSrc}
                </div>
                
                <div class="p-5 flex flex-col flex-grow">
                    <h4 class="font-bold text-theme-main text-sm m-0 mb-3 truncate">${item.Name}</h4>
                    <span class="text-[9px] uppercase tracking-widest font-bold px-2 py-0.5 rounded ${colors.badge} border inline-block mb-3 self-start">${item.Type}</span>
                    <p class="text-xs text-theme-muted leading-relaxed m-0 flex-grow">
                        ${displayDesc}
                    </p>
                    <a href="${item.Url}" target="_blank" rel="noopener noreferrer" class="mt-5 w-full text-center py-2.5 px-4 rounded-xl text-[10px] font-bold uppercase tracking-widest transition-colors border border-theme bg-theme-alternate hover:bg-btc-orange/10 hover:text-btc-orange text-theme-main">
                        ${isTruncated ? 'Leer más' : 'Ver enlace'}
                    </a>
                </div>
            </div>
        `;
    };

    const setupSliderControls = (container: HTMLElement | null, prevBtnId: string, nextBtnId: string) => {
        const btnPrev = document.getElementById(prevBtnId) as HTMLButtonElement;
        const btnNext = document.getElementById(nextBtnId) as HTMLButtonElement;
        if (!container || !btnPrev || !btnNext) return;

        const updateArrows = () => {
            const maxScroll = container.scrollWidth - container.clientWidth;
            btnPrev.disabled = container.scrollLeft <= 5;
            btnNext.disabled = container.scrollLeft >= maxScroll - 5;
        };

        container.addEventListener('scroll', updateArrows, { passive: true });
        updateArrows();

        btnPrev.addEventListener('click', () => {
            const item = container.querySelector('.mod-item') as HTMLElement;
            const cardWidth = item ? item.offsetWidth + 20 : 260; 
            container.scrollBy({ left: -(cardWidth * 2), behavior: 'smooth' });
        });
        btnNext.addEventListener('click', () => {
            const item = container.querySelector('.mod-item') as HTMLElement;
            const cardWidth = item ? item.offsetWidth + 20 : 260;
            container.scrollBy({ left: (cardWidth * 2), behavior: 'smooth' });
        });
    };

    try {
        const { mods, datapacks } = await getModsAndDatapacks();
        if (datapacks.length > 0 && dpList && dpSection) {
            dpList.innerHTML = datapacks.map((item: any) => createModCardHTML(item)).join('');
            dpSection.classList.remove('hidden');
            dpSection.classList.add('flex');
            setupSliderControls(dpList, 'dp-prev', 'dp-next');
        }

        if (mods.length > 0 && modsList && modsSection) {
            modsList.innerHTML = mods.map((item: any) => createModCardHTML(item)).join('');
            modsSection.classList.remove('hidden');
            modsSection.classList.add('flex');
            setupSliderControls(modsList, 'mod-prev', 'mod-next');
        }
        
        if(loadingEl) loadingEl.style.display = 'none';
    } catch (err) {
        console.error("Error cargando mods:", err);
        if(loadingEl) loadingEl.innerHTML = "<span class='text-red-500 font-bold'>Error al conectar con la base de datos.</span>";
    }
});
</script>